import React, { Component } from 'react'

export class VSMode extends Component {
    render() {
        return (
            <div>
                VSMODE
            </div>
        )
    }
}

export default VSMode
